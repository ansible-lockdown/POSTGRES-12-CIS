---
# 1.1
get_repo_list: "yum repolist all | grep enabled"

# 1.2 + 1.3
get_postgres_binary_list: "yum info $(rpm -qa|grep postgres) | egrep '^Name|^Version|^From'"

# 1.5
pgs12cis_1_5_db_initialize: 'PGSETUP_INITDB_OPTIONS="-k" /usr/pgsql-12/bin/postgresql-12-setup initdb'

# 3.2
pgs12cis_3_2_pgaudit_install: "pgaudit14_12"
